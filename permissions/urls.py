from django.conf.urls import url
from permissions.views import *

app_name = 'permissions'
urlpatterns =[
    # url(r'create/$', PermissionGroupsBetterCreateView.as_view(), name='permissions_create'),
    # url(r'update/(?P<pk>\d+)/$', PermissionGroupsBetterUpdateView.as_view(), name='permissions_update'),
    # url(r'permissiongroups/$', PermissionGroupsListView.as_view(), name='permissions_list'),

]

